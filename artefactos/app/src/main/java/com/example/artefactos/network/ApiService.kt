package com.example.artefactos.network

//import com.example.artefactos.network.models.ExampleResponse
import retrofit2.http.GET
import retrofit2.http.Path

interface ApiService {

    // Ejemplo de endpoint: obtiene lista de posts
//    @GET("posts")
//    suspend fun getPosts(): List<ExampleResponse>

    // Ejemplo con parámetro
//    @GET("posts/{id}")
//    suspend fun getPostById(@Path("id") id: Int): ExampleResponse
}
